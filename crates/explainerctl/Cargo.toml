[package]
name = "explainerctl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1"
explainer_core = { path = "../explainer_core" }
serde_json = "1"
