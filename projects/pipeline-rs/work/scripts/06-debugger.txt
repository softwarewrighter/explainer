The record at a time debugger lets you step through execution. Each record moves through the pipeline one stage at a time. Set watches to monitor data at any point. Set breakpoints to pause. You see exactly how each record is filtered, transformed, or passed through.
