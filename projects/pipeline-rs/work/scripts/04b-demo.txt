The docs describe a record at a time executor. The help flag shows usage. Employee records are eighty byte fixed width lines. The change pipe renames sales in every record. Verbose mode adds file paths and record counts. An alias shortens the command for inline use, passing pipe specs and data directly.