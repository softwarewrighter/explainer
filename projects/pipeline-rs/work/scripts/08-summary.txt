So you have seen three ways to use mainframe pipelines in Rust. The command line tool runs pipeline specs against data. The web tutorials walk through each stage. And the interactive debugger lets you step through records one at a time.