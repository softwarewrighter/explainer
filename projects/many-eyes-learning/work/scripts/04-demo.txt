Here is the web interface with five scouts training in parallel. Each grid shows a different scout exploring from start to goal. Watch as I speed up the training. The heatmaps show where each scout visits most. As learning progresses, policy arrows appear pointing toward the goal. The learning curve tracks average steps, dropping as scouts get more efficient. After fifty episodes, we hit ninety four percent success.