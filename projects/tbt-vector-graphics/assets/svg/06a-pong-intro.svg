<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1920 1080">
  <defs>
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="4" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Dark background -->
  <rect width="1920" height="1080" fill="#0a0a0a"/>

  <!-- Vector Arcade Platform header image -->
  <image href="../images/vector-arcade-platform.png" x="310" y="130" width="1300" height="100"/>

  <!-- PONG - Large centered vector text (all caps) -->
  <g fill="none" stroke="#ffffff" stroke-width="8" stroke-linecap="square" filter="url(#glow)" transform="translate(960, 540)">
    <!-- P -->
    <path d="M-300,-120 L-300,120 M-300,-120 L-200,-120 Q-140,-120 -140,-60 Q-140,0 -200,0 L-300,0"/>
    <!-- O -->
    <path d="M-80,-120 L80,-120 L80,120 L-80,120 Z"/>
    <!-- N -->
    <path d="M160,-120 L160,120 M160,-120 L300,120 M300,-120 L300,120"/>
    <!-- G -->
    <path d="M460,-120 L380,-120 L380,120 L460,120 L460,0 L420,0"/>
  </g>

  <!-- Decorative pong elements -->
  <g stroke="#ffffff" stroke-width="4" fill="none" opacity="0.3">
    <!-- Center line -->
    <line x1="960" y1="850" x2="960" y2="870" stroke-dasharray="20,10"/>
    <!-- Paddles -->
    <rect x="100" y="820" width="15" height="80"/>
    <rect x="1805" y="820" width="15" height="80"/>
    <!-- Ball -->
    <rect x="950" y="890" width="20" height="20"/>
  </g>

  <!-- Avatar safe zone kept clear: x=1400-1920, y=800-1080 -->
</svg>
