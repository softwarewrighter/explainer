Watch as I assemble this program that adds two numbers. The machine code appears below with hex encodings. I step through instruction by instruction. The accumulator loads five, adds seven, and the result twelve appears with the positive flag lit. Now a more advanced example using memory mapped registers. The eleven thirty stores index registers at addresses one, two, and three. Watch the memory grid highlight as values flow into these locations. The indexed addressing loads from address fifty plus the index register.
