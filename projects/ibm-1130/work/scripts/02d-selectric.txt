The ten fifty three console printer uses Selectric type ball technology, printing at about fifteen characters per second. Watch as it outputs an assembler listing on greenbar paper. Each line shows statement number, memory address, hex object code, labels, and source. This sample implements a counter loop with arithmetic instructions. The ball element rotates and tilts to strike each character through the ribbon. The green and white stripes helped operators read across long lines of output.
